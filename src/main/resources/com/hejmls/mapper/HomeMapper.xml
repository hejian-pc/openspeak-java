<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hejmls.mapper.HomeMapper">
    <select id="homeArticle" resultType="com.hejmls.pojo.Article">
        select article_id, user_id, category_id, title, content, publish_date, last_updated_date from articles
    </select>
    <select id="homeArticlebyarticlesId" resultType="com.hejmls.pojo.Article">
        select article_id, user_id, category_id, title, content, publish_date, last_updated_date from articles where article_id = #{articleId}
    </select>
    <select id="homeArticlebycategoryId" resultType="com.hejmls.pojo.Article">
        select article_id, user_id, category_id, title, content, publish_date, last_updated_date from articles where category_id = #{categoryId}
    </select>
</mapper>